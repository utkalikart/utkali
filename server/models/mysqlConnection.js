var mysql = require('mysql');
var config = require('../config/environment');

/*
connection = module.exports = mysql.createConnection(config.mysql);
connection.connect(function(err) {
  if (err) {
    console.error('error connecting: ' + err.stack);
    return;
  }
});*/
